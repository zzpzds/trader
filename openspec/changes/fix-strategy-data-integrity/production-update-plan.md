# AMKR production correction — read-only preparation

## Status and boundary

- **Status:** DONE — payload preparation only; production write is pending explicit user confirmation.
- **Strategy ID validated:** `bd181ef3-298c-487c-bc02-c0bb69664912` (exact match)
- **Read-only GET response completion time (Asia/Shanghai):** `2026-08-17T14:22:46+0800 (CST)`
- **Snapshot `updatedAt`:** `2026-07-30T11:19:20.720Z`
- **Production writes performed:** none. No PUT/POST/PATCH/DELETE, deployment, or task trigger was issued.

The successful snapshot is internally inconsistent: `symbols` still contains `AIQ`, `content` already describes AMKR with T2 parameters, and `script` still configures AIQ as T1. The prepared partial update changes only the three permitted fields.

## Verbatim snapshot and rollback JSON

This JSON is both the verbatim snapshot of the three in-scope fields and the complete rollback payload. It contains **only** `symbols`, `content`, and `script`.

```json
{
  "symbols": ["NVDA", "GOOGL", "MSFT", "META", "AIQ"],
  "content": "## 策略总结\n  AI板块等权配置策略，采用分级“沿趋势追跌”的分批\n  建仓模式。NVDA、GOOGL、MSFT、META采用T1策略；\n  AMKR采用T2高波动策略。每只标的目标仓位均为\n  $10,000，总预算$50,000，各标的独立执行建仓、加\n  仓及参考价重置规则。\n\n  ## 采用参数\n\n  ### T1参数：NVDA / GOOGL / MSFT / META\n  - 目标仓位：$10,000/只\n  - 首次建仓：30%（$3,000）\n  - 单次加仓：14%（$1,400）\n  - 加仓触发：股价≤参考价×0.90（下跌10%）\n  - 恢复重置：股价≥参考价×1.15（上涨15%）\n  - 最大加仓次数：5次\n\n  ### T2参数：AMKR\n  - 目标仓位：$10,000\n  - 首次建仓：20%（$2,000）\n  - 单次加仓：10%（$1,000）\n  - 加仓触发：股价≤参考价×0.85（下跌15%）\n  - 恢复重置：股价≥参考价×1.20（上涨20%）\n  - 最大加仓次数：8次\n\n  ## 涉及标的\n  - **NVDA**：GPU算力龙头，采用T1策略\n  - **GOOGL**：搜索、GCP、TPU、Gemini、YouTube及\n  Waymo，采用T1策略\n  - **MSFT**：Azure、Copilot及OpenAI生态，采用T1\n  策略\n  - **META**：Llama、广告AI及自研ASIC，采用T1策略\n  - **AMKR**：半导体封装与测试服务商，采用T2高波\n  动策略\n\n  ## 买卖规则\n\n  ### NVDA / GOOGL / MSFT / META\n  - 无持仓时，首次买入目标仓位的30%，即$3,000\n  - 当前价≤参考价×0.90时，加仓目标仓位的14%，即\n  $1,400\n  - 最多加仓5次\n  - 每次买入后立即将参考价更新为当前成交价\n  - 当前价≥参考价×1.15时，将参考价更新为当前价，\n  不进行交易\n\n  ### AMKR\n  - 无持仓时，首次买入目标仓位的20%，即$2,000\n  - 当前价≤参考价×0.85时，加仓目标仓位的10%，即\n  $1,000\n  - 最多加仓8次，首仓与全部加仓完成后累计投入\n  $10,000\n  - 每次买入后立即将参考价更新为当前成交价\n  - 当前价≥参考价×1.20时，将参考价更新为当前价，\n  不进行交易\n\n  ## 卖出规则\n  无主动清仓或止盈卖出机制。上涨达到恢复阈值时仅\n  重置参考价，调整后续加仓基准。\n\n  ## 切换逻辑\n  五只标的保持每只$10,000的目标仓位，\n  各自独立运行，不进行标的间资金轮动。",
  "script": "\"\"\"\nAI 板块策略跟踪脚本\n==================\n持仓（总预算 $50,000，每只 $10,000 等权）：\n  - NVDA   GPU 算力龙头\n  - GOOGL  搜索 + GCP + TPU + Gemini + YouTube + Waymo\n  - MSFT   Azure + Copilot + OpenAI 战略持股\n  - META   Llama + 广告 AI + 自研 ASIC\n  - AIQ    Global X AI ETF (~90 只，含 AMZN/AMD/AVGO/TSM/AAPL 间接暴露)\n\n策略（T1 大盘）：\n  - 初仓 30% = $3,000\n  - 加仓触发：股价 ≤ ref × 0.90\n  - 加仓金额：14% = $1,400（最多 5 次）\n  - 恢复重置：股价 ≥ ref × 1.15 → 更新 ref（不交易）\n  - 注意：每次买入都会刷新 ref，等于\"沿趋势追跌\"模式\n\n用法：\n  python ai_strategy.py                         # 检查信号\n  python ai_strategy.py buy GOOGL 3000 195.50   # 记录买入\n  python ai_strategy.py reset NVDA              # 手动触发恢复重置\n\"\"\"\n\nimport json\nimport sys\nimport urllib.request\nfrom datetime import datetime\nfrom pathlib import Path\n\nSTATE_FILE = Path(__file__).parent / \"ai_state.json\"\n\n# ── 策略参数 ─────────────────────────────────────────────────────────────────\nSTOCKS = {\n    \"NVDA\":  {\"target\": 10_000, \"tier\": \"T1\"},\n    \"GOOGL\": {\"target\": 10_000, \"tier\": \"T1\"},\n    \"MSFT\":  {\"target\": 10_000, \"tier\": \"T1\"},\n    \"META\":  {\"target\": 10_000, \"tier\": \"T1\"},\n    \"AIQ\":   {\"target\": 10_000, \"tier\": \"T1\"},\n}\n\nT1 = {\n    \"initial_pct\":      0.30,\n    \"add_pct\":          0.14,\n    \"drop_trigger\":     0.10,   # -10% 触发加仓\n    \"recovery_trigger\": 0.15,   # +15% 触发恢复重置\n    \"max_adds\":         5,\n}\n\n# ── 价格抓取 ─────────────────────────────────────────────────────────────────\ndef fetch_price(ticker: str) -> float:\n    url = f\"https://query2.finance.yahoo.com/v8/finance/chart/{ticker}?interval=1d&range=1d\"\n    req = urllib.request.Request(url, headers={\"User-Agent\": \"Mozilla/5.0\"})\n    with urllib.request.urlopen(req, timeout=15) as r:\n        data = json.loads(r.read())\n    return float(data[\"chart\"][\"result\"][0][\"meta\"][\"regularMarketPrice\"])\n\n# ── 状态持久化 ───────────────────────────────────────────────────────────────\ndef empty_position():\n    return {\n        \"ref_price\":   None,\n        \"shares\":      0.0,\n        \"invested\":    0.0,\n        \"add_count\":   0,\n        \"last_action\": None,\n        \"history\":     [],\n    }\n\ndef load_state() -> dict:\n    if STATE_FILE.exists():\n        raw = json.loads(STATE_FILE.read_text())\n        result = {}\n        for t in STOCKS:\n            pos = raw.get(t, empty_position())\n            for k, v in empty_position().items():\n                pos.setdefault(k, v)\n            result[t] = pos\n        return result\n    return {t: empty_position() for t in STOCKS}\n\ndef save_state(state: dict) -> None:\n    STATE_FILE.write_text(json.dumps(state, indent=2, ensure_ascii=False))\n\n# ── 信号检查 ─────────────────────────────────────────────────────────────────\ndef check_signals(state: dict) -> None:\n    print(f\"\\n=== AI 板块状态 — {datetime.now():%Y-%m-%d %H:%M} ===\\n\")\n    print(f\"{'股票':<6} {'参考价':>10} {'当前价':>10} {'触发价':>10} \"\n          f\"{'加仓':>6} {'已投入':>10}  操作建议\")\n    print(\"─\" * 88)\n\n    total_invested = 0.0\n    total_value    = 0.0\n    actions = []\n\n    for ticker, cfg in STOCKS.items():\n        s = state.get(ticker, empty_position())\n        try:\n            current = fetch_price(ticker)\n        except Exception as e:\n            print(f\"{ticker:<6} 价格获取失败：{e}\")\n            continue\n\n        target  = cfg[\"target\"]\n        ref     = s[\"ref_price\"]\n        shares  = s[\"shares\"]\n        invested = s[\"invested\"]\n        adds    = s[\"add_count\"]\n        cur_value = shares * current\n\n        total_invested += invested\n        total_value    += cur_value\n\n        # 未建仓\n        if ref is None:\n            init_amt = target * T1[\"initial_pct\"]\n            action = f\"⏳ 建首仓 ${init_amt:,.0f}（≈{init_amt/current:.2f} 股）\"\n            actions.append((ticker, \"buy_initial\", init_amt, current))\n            print(f\"{ticker:<6} {'—':>10} {f'${current:.2f}':>10} {'—':>10} \"\n                  f\"{'0/5':>6} {'$0':>10}  {action}\")\n            continue\n\n        drop_price     = ref * (1 - T1[\"drop_trigger\"])\n        recovery_price = ref * (1 + T1[\"recovery_trigger\"])\n\n        # 已满仓\n        if adds >= T1[\"max_adds\"]:\n            status = \"✅ 已满仓持有\"\n        # 加仓信号\n        elif current <= drop_price:\n            add_amt = target * T1[\"add_pct\"]\n            status = f\"🔴 加仓信号！加仓 ${add_amt:,.0f}（≈{add_amt/current:.2f} 股）\"\n            actions.append((ticker, \"add\", add_amt, current))\n        # 恢复重置信号\n        elif current >= recovery_price:\n            status = f\"🟢 恢复重置！更新 ref → ${current:.2f}\"\n            actions.append((ticker, \"reset\", 0, current))\n        else:\n            pct_to_trigger = (current - drop_price) / current * 100\n            status = f\"持有（距触发 -{pct_to_trigger:.1f}%）\"\n\n        print(f\"{ticker:<6} {f'${ref:.2f}':>10} {f'${current:.2f}':>10} \"\n              f\"{f'${drop_price:.2f}':>10} {f'{adds}/5':>6} \"\n              f\"{f'${invested:,.0f}':>10}  {status}\")\n\n    print(\"─\" * 88)\n    print(f\"AI 板块累计投入：${total_invested:>10,.2f}\")\n    print(f\"AI 板块当前市值：${total_value:>10,.2f}\")\n    if total_invested > 0:\n        ret = (total_value / total_invested - 1) * 100\n        print(f\"AI 板块未实现收益：{ret:+.2f}%\")\n\n    if actions:\n        print(\"\\n⚠️  待执行动作：\")\n        for t, a, amt, p in actions:\n            if a == \"buy_initial\":\n                print(f\"  • {t} 建首仓 ${amt:,.0f} → 执行后运行: \"\n                      f\"python ai_strategy.py buy {t} {amt:.0f} <成交价>\")\n            elif a == \"add\":\n                print(f\"  • {t} 加仓 ${amt:,.0f} → 执行后运行: \"\n                      f\"python ai_strategy.py buy {t} {amt:.0f} <成交价>\")\n            elif a == \"reset\":\n                print(f\"  • {t} 重置参考价 → 运行: \"\n                      f\"python ai_strategy.py reset {t}\")\n\n# ── 命令处理 ─────────────────────────────────────────────────────────────────\ndef cmd_buy(state: dict, ticker: str, usd_amt: float, price: float) -> None:\n    if ticker not in STOCKS:\n        print(f\"未知股票：{ticker}\")\n        return\n    s = state.setdefault(ticker, empty_position())\n    is_initial = s[\"ref_price\"] is None\n    s[\"shares\"]   += usd_amt / price\n    s[\"invested\"] += usd_amt\n    s[\"ref_price\"] = price\n    if not is_initial:\n        s[\"add_count\"] += 1\n    s[\"last_action\"] = datetime.now().strftime(\"%Y-%m-%d\")\n    s[\"history\"].append({\n        \"date\": s[\"last_action\"],\n        \"type\": \"initial\" if is_initial else f\"add#{s['add_count']}\",\n        \"usd\":  usd_amt,\n        \"price\": price,\n    })\n    save_state(state)\n    print(f\"✅ 已记录：{ticker} 买入 ${usd_amt:,.2f} @ ${price:.2f}  \"\n          f\"（持股 {s['shares']:.4f}，加仓次数 {s['add_count']}/{T1['max_adds']}）\")\n\ndef cmd_reset(state: dict, ticker: str) -> None:\n    if ticker not in STOCKS:\n        print(f\"未知股票：{ticker}\")\n        return\n    s = state.setdefault(ticker, empty_position())\n    if s[\"ref_price\"] is None:\n        print(f\"{ticker} 未建仓，无需重置\")\n        return\n    try:\n        current = fetch_price(ticker)\n    except Exception as e:\n        print(f\"价格获取失败：{e}\")\n        return\n    old_ref = s[\"ref_price\"]\n    s[\"ref_price\"] = current\n    s[\"last_action\"] = datetime.now().strftime(\"%Y-%m-%d\")\n    s[\"history\"].append({\n        \"date\": s[\"last_action\"],\n        \"type\": \"recovery_reset\",\n        \"old_ref\": old_ref,\n        \"new_ref\": current,\n    })\n    save_state(state)\n    print(f\"✅ {ticker} 参考价重置：${old_ref:.2f} → ${current:.2f}\")\n\ndef main():\n    state = load_state()\n    args  = sys.argv[1:]\n\n    if not args:\n        check_signals(state)\n        return\n\n    cmd = args[0]\n    if cmd == \"buy\" and len(args) == 4:\n        cmd_buy(state, args[1].upper(), float(args[2]), float(args[3]))\n    elif cmd == \"reset\" and len(args) == 2:\n        cmd_reset(state, args[1].upper())\n    else:\n        print(__doc__)\n\nif __name__ == \"__main__\":\n    main()\n"
}
```

## Prepared partial PUT payload — do not send

This complete JSON object contains **only** the three permitted fields. The four existing T1 rules in `content` are preserved verbatim. The script keeps those T1 values unchanged and adds the distinct AMKR/T2 parameters and tier selection needed to make the script agree with the content.

```json
{
  "symbols": ["NVDA", "GOOGL", "MSFT", "META", "AMKR"],
  "content": "## 策略总结\n  AI板块等权配置策略，采用分级“沿趋势追跌”的分批\n  建仓模式。NVDA、GOOGL、MSFT、META采用T1策略；\n  AMKR采用T2高波动策略。每只标的目标仓位均为\n  $10,000，总预算$50,000，各标的独立执行建仓、加\n  仓及参考价重置规则。\n\n  ## 采用参数\n\n  ### T1参数：NVDA / GOOGL / MSFT / META\n  - 目标仓位：$10,000/只\n  - 首次建仓：30%（$3,000）\n  - 单次加仓：14%（$1,400）\n  - 加仓触发：股价≤参考价×0.90（下跌10%）\n  - 恢复重置：股价≥参考价×1.15（上涨15%）\n  - 最大加仓次数：5次\n\n  ### T2参数：AMKR\n  - 目标仓位：$10,000\n  - 首次建仓：20%（$2,000）\n  - 单次加仓：10%（$1,000）\n  - 加仓触发：股价≤参考价×0.85（下跌15%）\n  - 恢复重置：股价≥参考价×1.20（上涨20%）\n  - 最大加仓次数：8次\n\n  ## 涉及标的\n  - **NVDA**：GPU算力龙头，采用T1策略\n  - **GOOGL**：搜索、GCP、TPU、Gemini、YouTube及\n  Waymo，采用T1策略\n  - **MSFT**：Azure、Copilot及OpenAI生态，采用T1\n  策略\n  - **META**：Llama、广告AI及自研ASIC，采用T1策略\n  - **AMKR**：半导体封装与测试服务商，采用T2高波\n  动策略\n\n  ## 买卖规则\n\n  ### NVDA / GOOGL / MSFT / META\n  - 无持仓时，首次买入目标仓位的30%，即$3,000\n  - 当前价≤参考价×0.90时，加仓目标仓位的14%，即\n  $1,400\n  - 最多加仓5次\n  - 每次买入后立即将参考价更新为当前成交价\n  - 当前价≥参考价×1.15时，将参考价更新为当前价，\n  不进行交易\n\n  ### AMKR\n  - 无持仓时，首次买入目标仓位的20%，即$2,000\n  - 当前价≤参考价×0.85时，加仓目标仓位的10%，即\n  $1,000\n  - 最多加仓8次，首仓与全部加仓完成后累计投入\n  $10,000\n  - 每次买入后立即将参考价更新为当前成交价\n  - 当前价≥参考价×1.20时，将参考价更新为当前价，\n  不进行交易\n\n  ## 卖出规则\n  无主动清仓或止盈卖出机制。上涨达到恢复阈值时仅\n  重置参考价，调整后续加仓基准。\n\n  ## 切换逻辑\n  五只标的保持每只$10,000的目标仓位，\n  各自独立运行，不进行标的间资金轮动。",
  "script": "\"\"\"\nAI 板块策略跟踪脚本\n==================\n持仓（总预算 $50,000，每只 $10,000 等权）：\n  - NVDA   GPU 算力龙头\n  - GOOGL  搜索 + GCP + TPU + Gemini + YouTube + Waymo\n  - MSFT   Azure + Copilot + OpenAI 战略持股\n  - META   Llama + 广告 AI + 自研 ASIC\n  - AMKR   半导体封装与测试服务商（T2 高波动）\n\n策略：\n  - NVDA / GOOGL / MSFT / META：T1，初仓 30% = $3,000；加仓 14% = $1,400（最多 5 次）；股价 ≤ ref × 0.90 加仓；股价 ≥ ref × 1.15 更新 ref。\n  - AMKR：T2，目标仓位 $10,000；初仓 20% = $2,000；后续每次加仓 10% = $1,000（最多 8 次）；股价 ≤ ref × 0.85 加仓；股价 ≥ ref × 1.20 更新 ref。\n\"\"\"\n\nimport json\nimport sys\nimport urllib.request\nfrom datetime import datetime\nfrom pathlib import Path\n\nSTATE_FILE = Path(__file__).parent / \"ai_state.json\"\n\nSTOCKS = {\n    \"NVDA\":  {\"target\": 10_000, \"tier\": \"T1\"},\n    \"GOOGL\": {\"target\": 10_000, \"tier\": \"T1\"},\n    \"MSFT\":  {\"target\": 10_000, \"tier\": \"T1\"},\n    \"META\":  {\"target\": 10_000, \"tier\": \"T1\"},\n    \"AMKR\":  {\"target\": 10_000, \"tier\": \"T2\"},\n}\n\nT1 = {\n    \"initial_pct\":      0.30,\n    \"add_pct\":          0.14,\n    \"drop_trigger\":     0.10,   # -10% 触发加仓\n    \"recovery_trigger\": 0.15,   # +15% 触发恢复重置\n    \"max_adds\":         5,\n}\n\nT2 = {\n    \"initial_pct\":      0.20,\n    \"add_pct\":          0.10,\n    \"drop_trigger\":     0.15,   # -15% 触发加仓\n    \"recovery_trigger\": 0.20,   # +20% 触发恢复重置\n    \"max_adds\":         8,\n}\n\nTIERS = {\"T1\": T1, \"T2\": T2}\n\ndef fetch_price(ticker: str) -> float:\n    url = f\"https://query2.finance.yahoo.com/v8/finance/chart/{ticker}?interval=1d&range=1d\"\n    req = urllib.request.Request(url, headers={\"User-Agent\": \"Mozilla/5.0\"})\n    with urllib.request.urlopen(req, timeout=15) as r:\n        data = json.loads(r.read())\n    return float(data[\"chart\"][\"result\"][0][\"meta\"][\"regularMarketPrice\"])\n\ndef empty_position():\n    return {\"ref_price\": None, \"shares\": 0.0, \"invested\": 0.0, \"add_count\": 0, \"last_action\": None, \"history\": []}\n\ndef load_state() -> dict:\n    if STATE_FILE.exists():\n        raw = json.loads(STATE_FILE.read_text())\n        return {ticker: {**empty_position(), **raw.get(ticker, {})} for ticker in STOCKS}\n    return {ticker: empty_position() for ticker in STOCKS}\n\ndef save_state(state: dict) -> None:\n    STATE_FILE.write_text(json.dumps(state, indent=2, ensure_ascii=False))\n\ndef check_signals(state: dict) -> None:\n    print(f\"\\n=== AI 板块状态 — {datetime.now():%Y-%m-%d %H:%M} ===\\n\")\n    for ticker, cfg in STOCKS.items():\n        params = TIERS[cfg[\"tier\"]]\n        s = state.get(ticker, empty_position())\n        try:\n            current = fetch_price(ticker)\n        except Exception as e:\n            print(f\"{ticker}: 价格获取失败：{e}\")\n            continue\n        target, ref, adds = cfg[\"target\"], s[\"ref_price\"], s[\"add_count\"]\n        if ref is None:\n            amount = target * params[\"initial_pct\"]\n            print(f\"{ticker} ({cfg['tier']}): 建首仓 ${amount:,.0f}\")\n        elif adds >= params[\"max_adds\"]:\n            print(f\"{ticker} ({cfg['tier']}): 已满仓持有\")\n        elif current <= ref * (1 - params[\"drop_trigger\"]):\n            amount = target * params[\"add_pct\"]\n            print(f\"{ticker} ({cfg['tier']}): 加仓 ${amount:,.0f}\")\n        elif current >= ref * (1 + params[\"recovery_trigger\"]):\n            print(f\"{ticker} ({cfg['tier']}): 更新 ref → ${current:.2f}\")\n        else:\n            print(f\"{ticker} ({cfg['tier']}): 持有\")\n\ndef cmd_buy(state: dict, ticker: str, usd_amt: float, price: float) -> None:\n    if ticker not in STOCKS:\n        print(f\"未知股票：{ticker}\")\n        return\n    s = state.setdefault(ticker, empty_position())\n    is_initial = s[\"ref_price\"] is None\n    s[\"shares\"] += usd_amt / price\n    s[\"invested\"] += usd_amt\n    s[\"ref_price\"] = price\n    if not is_initial:\n        s[\"add_count\"] += 1\n    s[\"last_action\"] = datetime.now().strftime(\"%Y-%m-%d\")\n    s[\"history\"].append({\"date\": s[\"last_action\"], \"type\": \"initial\" if is_initial else f\"add#{s['add_count']}\", \"usd\": usd_amt, \"price\": price})\n    save_state(state)\n\ndef cmd_reset(state: dict, ticker: str) -> None:\n    if ticker not in STOCKS or state.get(ticker, empty_position())[\"ref_price\"] is None:\n        return\n    current = fetch_price(ticker)\n    state[ticker][\"ref_price\"] = current\n    state[ticker][\"last_action\"] = datetime.now().strftime(\"%Y-%m-%d\")\n    save_state(state)\n\ndef main():\n    state, args = load_state(), sys.argv[1:]\n    if not args:\n        check_signals(state)\n    elif args[0] == \"buy\" and len(args) == 4:\n        cmd_buy(state, args[1].upper(), float(args[2]), float(args[3]))\n    elif args[0] == \"reset\" and len(args) == 2:\n        cmd_reset(state, args[1].upper())\n    else:\n        print(__doc__)\n\nif __name__ == \"__main__\":\n    main()\n"
}
```

## Required audit checklist for a future confirmed PUT

- [x] Snapshot response is JSON and its `id` exactly matches the target strategy.
- [x] Proposed `symbols` exactly equal `[NVDA, GOOGL, MSFT, META, AMKR]`, in that order.
- [x] The proposed `symbols`, `content`, and `script` contain no `AIQ` reference.
- [x] Both proposed `content` and `script` contain AMKR, T2, a $10,000 total position, a 20% first entry, and a 10% later add.
- [x] The other four symbols' T1 parameters remain verbatim in `content` and unchanged in the proposed script's `T1` object.
- [x] Both JSON objects contain only `symbols`, `content`, and `script`; no unrelated field can be overwritten.
- [ ] After a future confirmed PUT, immediately GET again and require `updatedAt` to differ from `2026-07-30T11:19:20.720Z` before treating the update as applied.
- [ ] After that GET, recheck exact symbols and no `AIQ` in all three target fields before any downstream action.

No payload in this document has been sent. A fresh GET must still be performed in the actual production write window before any future PUT, as required by the design.
